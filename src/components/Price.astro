---
interface Props {
  amount: number;
  currency?: string;
}
const { amount, currency = 'USD' } = Astro.props;
const formatted = new Intl.NumberFormat('en-US', { style: 'currency', currency }).format(amount);
---

<p class="price">{formatted}</p>
